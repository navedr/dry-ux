interface JQuery {
    notify(message: string, options: any): JQuery;
    noty: any;
}

interface Window {
    noty(options: any): JQuery;
    google: any;
    InfoBox: any;
    welcomeMessage: string;
    isLocal: boolean;
    devHost: string;
    $: any;
}

const $ = window.$;